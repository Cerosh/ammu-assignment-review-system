import { defineConfig, devices } from "@playwright/test";

export default defineConfig({
  testDir: "./tests",
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  reporter: [["html", { open: "never" }]],
  use: {
    baseURL: "http://localhost:3000",
    trace: "on-first-retry",
  },
  // Chrome primary, Firefox/Safari secondary (see .ai/TESTING.md Browser Support).
  // All three run locally and in .husky/pre-push. CI (.github/workflows/ci.yml)
  // only runs chromium+firefox — WebKit's Linux/CI build has repeatedly shown
  // debounced-UI timing flakes on hosted runners that don't reproduce locally,
  // so it's enforced pre-push instead (see .ai/TODO.md Backlog).
  projects: [
    {
      name: "chromium",
      use: { ...devices["Desktop Chrome"] },
    },
    {
      name: "firefox",
      use: { ...devices["Desktop Firefox"] },
    },
    {
      name: "webkit",
      use: { ...devices["Desktop Safari"] },
    },
  ],
  webServer: {
    command: "npm run build && npm run start",
    url: "http://localhost:3000",
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
});
